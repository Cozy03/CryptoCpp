#pragma once

#include <string>

/**
 * @file types.hpp
 * @brief Common type aliases for CryptoCpp project.
 */

namespace cryptcpp {

/**
 * @typedef Plaintext
 * @brief Alias for plaintext data type.
 */
using Plaintext = std::string;

/**
 * @typedef Ciphertext
 * @brief Alias for ciphertext data type.
 */
using Ciphertext = std::string;

} // namespace cryptcpp

